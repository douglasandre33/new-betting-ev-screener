# Agent Instructions

- TypeScript strict mode only.
- Do not use `any`.
- Prefer pure functions for math.
- Add tests for new logic.
- Keep required environment variables in `.env.example`.
- Never commit secrets.
